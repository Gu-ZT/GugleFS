{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the GugleFS configuration window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

