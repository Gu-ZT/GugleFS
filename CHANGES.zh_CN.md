# 更新日志

GugleFS 的重要变更记录在此。Release 描述会读取与应用版本匹配的章节。

## [Unreleased]

- 增加可移植 JSON 配置导入/导出，导出文件会移除凭据和私钥引用。
- 增加 Release 更新日志自动化，发布双语变更说明和附件到平台的下载表格。
- 增加仓库威胁模型以及贡献和发布不变量说明。

## [0.7.0] - 2026-07-29

- 将 macOS 挂载后端从 macFUSE 替换为纯用户空间的 FUSE-T 1.2.7。
- 在 macOS 安装包中内置经过校验的 FUSE-T 官方安装器、许可和归属声明。
- 增加 FUSE-T 运行时检测，并在缺失时提供应用内安装入口。
- 通过仓库内 pkg-config 元数据让 macOS CI 的 `fuser` 直接链接 `libfuse-t`。

## [0.6.0] - 2026-07-28

- 增加可选的开机自启动，启动后静默驻留系统托盘。
- 配置时自动检测 SFTP MFA，并禁止需要 MFA 的映射执行不受支持的自动挂载。
- 检测 Windows 已占用盘符，为新映射自动选择下一个可用盘符。
- 将桌面窗口和映射表单调整为更紧凑的 4:3 工作区。

## [0.5.0] - 2026-07-28

- 使用 Vue 3 重构配置界面，形成聚焦桌面操作的控制台布局。
- 增加映射状态卡片、挂载对话框、已保存凭据快捷挂载及响应式空白/错误状态。
- 重写中英文 README，补充架构、安全边界、使用方式和界面截图。

## [0.4.0] - 2026-07-28

- 支持使用一次性六位 TOTP 验证码手动完成 SFTP MFA 认证。
- 增加 SSH 传输 keepalive、SFTP session 静默重建及非 MFA 连接的安全重连。
- 改进映射卡片、应用品牌和挂载状态展示。

## [0.3.0] - 2026-07-28

- 为 FTP、FTPS、SFTP 和 WebDAV 增加系统代理发现及按映射忽略代理选项。
- 支持本地或粘贴 SSH 私钥，并将粘贴私钥保存到各平台安全凭据库。
- 修复 Windows 目录挂载冲突并完成首个跨平台发布配置。
- 引入侧边栏映射工作区和应用图标。

## [0.2.0] - 2026-07-28

- 在 Windows WinFsp 之外增加 Linux FUSE3 和 macOS FUSE 挂载。
- 增加 macOS Keychain 和 Linux Secret Service 凭据存储。
- 增加系统托盘、单实例保护、安全退出卸载及异常映射恢复。
- 增加有界元数据/目录缓存和按句柄隔离的顺序预读。
- 增加 Windows、Linux、macOS 发布打包及 macOS 未签名回退。

## [0.1.0] - 2026-07-28

- 建立 Tauri、Vue/TypeScript 和 Rust workspace 架构。
- 实现 WebDAV、FTP/FTPS 和 SFTP 远程文件系统操作。
- 支持 WinFsp 盘符/目录挂载并修复 WebDAV 中文路径。
- 增加启动 TOTP 2FA、安全凭据持久化和挂载状态恢复。
- 增加自动预发布及内置 WinFsp 运行时的 Windows 安装包。
