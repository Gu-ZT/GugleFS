# 更新日志

GugleFS 的重要变更记录在此。Release 描述会读取与应用版本匹配的章节。

## [Unreleased]

## [0.16.2] - 2026-08-03

- 修复 WinFsp 分页目录中首批结果之外的文件无法打开的问题；目录尚未枚举完整时会回退远端元数据查询，编辑器保存大目录文件不再收到 `EIO`。

## [0.16.1] - 2026-08-03

- 增加启动和锁定 TOTP 验证的工作区安全开关；关闭前必须验证当前验证码，关闭后启动和锁屏支持直接解锁。

## [0.16.0] - 2026-08-02

- SFTP 大目录改用基于游标的线性分页；WinFsp 与 FUSE 无需等待完整目录即可返回首批结果，在目录句柄内复用稳定快照并清理中断游标，同时取消会与前台浏览竞争连接的挂载时根目录预取，并通过更长的有界元数据缓存减少重复属性和缩略图请求。
- Explorer 元数据探测改用正在枚举的目录名称视图，不再发出串行 SFTP 元数据请求；目录未变化时，同一 WinFsp 句柄重复从头枚举会复用已有快照和远端游标，本进程成功创建、删除或改名后则会刷新快照。目录分页等待专用 `READDIR` channel 时不再持有主 SFTP 连接锁，前台元数据和文件操作可以并发继续。
- WinFsp 卷信息现在会立即返回缓存或兼容容量，并在后台异步刷新 SFTP `statvfs` 或 WebDAV 配额数据；Unix 挂载仍保留远端容量缓存和报告。

## [0.15.1] - 2026-07-29

- FTP/FTPS 临时上传文件改用非隐藏名称，兼容禁止点号开头文件名的 Pure-FTPd 等服务器。

## [0.15.0] - 2026-07-29

- 修正 WinFsp 覆盖已有文件时的受限写入和分配大小语义；SFTP 写入现在会同步回读校验每个已确认的数据范围，截断后也会确认远端长度。校验或传输失败会使该文件句柄持续返回错误，避免同名覆盖上传静默生成损坏文件。
- FTP/FTPS 写入先上传同目录临时文件并校验远端长度，再重命名替换目标，上传失败时保留原文件。

## [0.14.0] - 2026-07-29

- 挂载后异步预取根目录，合并同一路径的并发目录读取，并让 WinFsp 分页复用稳定的已排序快照；FTP 使用 `CWD` 校验映射根目录，避免为根元数据执行完整列表。

## [0.13.0] - 2026-07-29

- 短暂缓存文件系统空间查询，并将 WebDAV 配额 `PROPFIND` 与普通元数据请求分离，避免拖慢目录浏览。

## [0.12.0] - 2026-07-29

- 增加可关闭的启动自动检查与手动检查更新，并在 GitHub API 不可用时通过代理回退获取版本，发现新版本后可前往 Release 页面下载。
- 增加使用同一内存远端场景的平台原生 WinFsp 与 FUSE 回调一致性测试。
- 将远端可获取的创建、访问和修改时间分别投影到 WinFsp 与 FUSE 元数据，不再用当前时间替代未知值。
- 增加真实的 SFTP `statvfs` 和 WebDAV 配额容量报告，仅在协议不提供文件系统空间时保留兼容回退值。
- 增加 FTP MLST/MLSD 时间解析，并将 WebDAV HTTP/RFC 3339 日期统一为 Unix 时间，同时保留条件写入保护。

## [0.11.0] - 2026-07-29

- 增加完整的简体中文与英文界面资源并同步原生托盘菜单，支持按系统语言初始化、手动切换并持久化语言偏好。
- 增加真实 FTP/SFTP 容器集成测试，覆盖连接、目录、创建、范围读写、重命名、截断、刷写和删除行为。
- 兼容包含四位 UNIX mode fact 的 Pure-FTPd MLST/MLSD 响应。
- 明确当前同步写穿契约，以及分块、暂存替换、有界落盘和崩溃恢复的实施门槛。

## [0.10.0] - 2026-07-29

- 增加后端驱动的挂载生命周期事件，并让启动恢复和应用锁定共用显式卸载中状态。
- 增加每映射远程 I/O 限流、控制/传输分级超时、安全的一次重试和 FTP 会话恢复。
- 完善键盘操作、对话框焦点、带映射上下文的屏幕阅读器标签，以及挂载状态和错误的实时播报。
- 增加不含敏感信息的非正常退出检测，并在解锁恢复挂载后提示用户核对状态。

## [0.9.0] - 2026-07-29

- 增加经过认证的远程目录浏览器，用于选择 FTP、FTPS、SFTP 和 WebDAV 映射根目录。
- 增加脱敏 JSONL 操作日志、日志轮转和用户可导出的 JSON 诊断报告。
- 为 SFTP 映射增加跨平台 SSH Agent 认证，以及经过服务器当前密钥校验的 OpenSSH `known_hosts` 导入。
- 为 WebDAV 增加强 ETag/Last-Modified 条件写入、并发冲突检测、安全的零偏移写入和非零截断。
- 为 WebDAV 增加 Digest、Bearer Token、匿名和本地 PEM 客户端证书认证。
- 为 WinFsp 增加大小写不敏感且保留拼写的查找、冲突检测、Windows 文件名校验和基础属性投影。

## [0.8.0] - 2026-07-29

- 增加可移植 JSON 映射配置导入/导出，导出文件不包含凭据或私钥引用。
- 增加双语 Release 描述生成，并附带平台、架构、格式和文件下载表格。
- 增加仓库威胁模型、安全不变量以及贡献/发布说明。
- 修复 macOS FUSE-T CI 链接，并增加直接链接 `libfuse-t` 的本地兼容 pkg-config shim。

## [0.7.0] - 2026-07-29

- 将 macOS 挂载后端从 macFUSE 替换为纯用户空间的 FUSE-T 1.2.7。
- 在 macOS 安装包中内置经过校验的 FUSE-T 官方安装器、许可和归属声明。
- 增加 FUSE-T 运行时检测，并在缺失时提供应用内安装入口。
- 通过仓库内 pkg-config 元数据让 macOS CI 的 `fuser` 直接链接 `libfuse-t`。

## [0.6.0] - 2026-07-28

- 增加可选的开机自启动，启动后静默驻留系统托盘。
- 配置时自动检测 SFTP MFA，并禁止需要 MFA 的映射执行不受支持的自动挂载。
- 检测 Windows 已占用盘符，为新映射自动选择下一个可用盘符。
- 将桌面窗口和映射表单调整为更紧凑的 4:3 工作区。

## [0.5.0] - 2026-07-28

- 使用 Vue 3 重构配置界面，形成聚焦桌面操作的控制台布局。
- 增加映射状态卡片、挂载对话框、已保存凭据快捷挂载及响应式空白/错误状态。
- 重写中英文 README，补充架构、安全边界、使用方式和界面截图。

## [0.4.0] - 2026-07-28

- 支持使用一次性六位 TOTP 验证码手动完成 SFTP MFA 认证。
- 增加 SSH 传输 keepalive、SFTP session 静默重建及非 MFA 连接的安全重连。
- 改进映射卡片、应用品牌和挂载状态展示。

## [0.3.0] - 2026-07-28

- 为 FTP、FTPS、SFTP 和 WebDAV 增加系统代理发现及按映射忽略代理选项。
- 支持本地或粘贴 SSH 私钥，并将粘贴私钥保存到各平台安全凭据库。
- 修复 Windows 目录挂载冲突并完成首个跨平台发布配置。
- 引入侧边栏映射工作区和应用图标。

## [0.2.0] - 2026-07-28

- 在 Windows WinFsp 之外增加 Linux FUSE3 和 macOS FUSE 挂载。
- 增加 macOS Keychain 和 Linux Secret Service 凭据存储。
- 增加系统托盘、单实例保护、安全退出卸载及异常映射恢复。
- 增加有界元数据/目录缓存和按句柄隔离的顺序预读。
- 增加 Windows、Linux、macOS 发布打包及 macOS 未签名回退。

## [0.1.0] - 2026-07-28

- 建立 Tauri、Vue/TypeScript 和 Rust workspace 架构。
- 实现 WebDAV、FTP/FTPS 和 SFTP 远程文件系统操作。
- 支持 WinFsp 盘符/目录挂载并修复 WebDAV 中文路径。
- 增加启动 TOTP 2FA、安全凭据持久化和挂载状态恢复。
- 增加自动预发布及内置 WinFsp 运行时的 Windows 安装包。
